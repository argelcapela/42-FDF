#include "../../fdf.h"

void    print_matrix(void **matrix)
{
    // declaring variables
    int     x;
    int     y;
    char    n;

    // initializing
    x = 0;
    y = 0;
    n = 'x';

    // iterating matrix and printing
    /*while (matrix[x])
    {
        while (matrix[x][y])
        {
            n = matrix[x][y];
            write(1, n, 1);
            y++;
        }
        y = 0;
        x ++;
    }*/

}